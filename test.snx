map results {
    type: .array;
    key: u32;
    value: u64;
    max: 8;
}

unit t5_if_test {
    section: "tracepoint/syscalls/sys_enter_execve";
    license: "GPL";

    reg a = 10;
    reg b = 20;

    if guard(a < b) {
        imm k0 = 0;
        heap p0 = results.lookup(k0);
        *p0 = 1;
    }

    return 0;
}
